# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx512m -XX:MaxMetaspaceSize=256m
# Heap + Metaspace raised well above the RN template defaults: R8 (full-mode
# shrink/obfuscate) and Android Lint on a multi-library New-Arch app exhaust the
# old 256m Metaspace with OutOfMemoryError. 4g heap / 1g Metaspace is comfortable.
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m -Dfile.encoding=UTF-8

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Use this property to specify which architecture you want to build.
# You can also override it from the CLI using
# ./gradlew <task> -PreactNativeArchitectures=x86_64
reactNativeArchitectures=armeabi-v7a,arm64-v8a,x86,x86_64

# React Native 0.86 forces the New Architecture for the app: the RN Gradle plugin
# hardcodes BuildConfig.IS_NEW_ARCHITECTURE_ENABLED = true (AgpConfiguratorUtils.kt),
# so this flag is kept true to stay consistent with the autolinked libraries' codegen.
# The Windows 260-char MAX_PATH failure that the New-Arch C++ codegen would otherwise
# hit (deep node_modules object paths mirrored under app/.cxx, rejected by the bundled
# ninja) is worked around via CMAKE_OBJECT_PATH_MAX in app/build.gradle. Hermes stays on.
newArchEnabled=true

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
hermesEnabled=true

# Use this property to enable edge-to-edge display support.
# This allows your app to draw behind system bars for an immersive UI.
# Note: Only works with ReactActivity and should not be used with custom Activity.
#
# On: we target SDK 36, and RN's updateEdgeToEdgeFeatureFlag() turns
# edge-to-edge on at runtime for any targetSdk >= 35 regardless of this flag.
# Leaving it false meant Android 15+ ran edge-to-edge while the JS side still
# believed it did not - so StatusBar props behaved differently there than on
# Android 14, for no stated reason. True makes every version behave the way
# 15+ already does. The app is inset-aware throughout (SafeAreaProvider,
# SafeAreaView, useSafeAreaInsets in the tab bar and paywall).
edgeToEdgeEnabled=true
