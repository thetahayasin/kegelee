<?xml version="1.0" encoding="utf-8"?>
<!--
  The training ring, which is what the phone app's own iconography is built on:
  a near-closed arc with the gap at the bottom, exactly like the session gauge.
  Drawn as a vector so one file covers every density on a watch.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:pathData="M54,30 A24,24 0 1,1 37,37"
        android:strokeColor="#C1FF72"
        android:strokeWidth="7"
        android:strokeLineCap="round"
        android:fillColor="@android:color/transparent" />
    <path
        android:pathData="M54,44 L54,64 M46,54 L62,54"
        android:strokeColor="#C1FF72"
        android:strokeWidth="5"
        android:strokeLineCap="round" />
</vector>
